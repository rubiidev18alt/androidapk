# Simple Android APK Template

This repository contains a minimal Android app template suitable for Android APK build tutorials.

## Build APK

```bash
./gradlew assembleDebug
```

The debug APK is generated at:

`app/build/outputs/apk/debug/app-debug.apk`
