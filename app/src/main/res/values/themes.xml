<resources>
    <!-- Host theme only. Real Material 3 styling now lives in Compose MaterialTheme. -->
    <style name="Theme.SimpleApk" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:colorAccent">@color/md3_expressive_primary</item>
        <item name="android:navigationBarColor">@color/md3_expressive_surface</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
